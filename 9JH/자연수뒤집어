class Solution {
  public int[] solution(long n) {
		char[] chArr = new StringBuilder(String.valueOf(n)).reverse().toString().toCharArray(); 
		int[] intArr = new int[chArr.length];
      
		for(int i=0; i<intArr.length; i++) intArr[i] = chArr[i] - 48; 
		return intArr;
	}
}
